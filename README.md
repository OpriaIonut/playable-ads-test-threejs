# Playable Ads Assignment

A TypeScript project using Vite and Three.js.

## Development

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:8080/` and opens the URL in the default browser automatically.

## Production build

```bash
npm run build
```